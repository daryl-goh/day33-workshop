export interface Cart {
    item: string;
    quantity: number;
    price: number;
}

export interface Details {
    name: string;
    address: string;
    email: string;
    phone: number;
    express: string;
    delivery: string;
}